A system to monitor the pH value of a system through a website, using ESP32 microcontroller

### To connect to a network
create a file named "credentials.txt" inside spiffs folder and add network credentials in the following format:  
```
networkName
password
```
then flash the spiffs partition(to the esp32's flash storage) to save the changes into the esp32's flash storage
